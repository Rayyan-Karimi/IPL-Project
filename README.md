# IPL-Project

- CSV to JSON conversions have been done already.
- After cloning the project, run each file from /src/server folder induvidually
- Result JSONs will get generated in the /public/output folder
- The resultant JSONs are already generated in the /public/output, and can be separated out from the folder for reference.
